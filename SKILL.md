---
name: qu-ai-wei
description: 给中文文本做去 AI 味、说人话、自然化、空话套话清理和按用户文风改写。适用于用户要求“去 AI 味”“说人话”“自然一点”“别像 ChatGPT”“删掉空话套话”“按我的文风”“快刀青衣文风”“广播站稿件”“口播稿”“公众号稿”“先标问题”“删改对照”“看看改了什么”等写作、改稿和审稿场景；先保住事实、数字、术语、引用、责任主体和作者已有经历，再清模板、压重复、补对象感、机制、判断或合适的结尾。
---

# 去味层 · 说人话

把文本拉回一个具体的人在当前场景下说话的状态。先保信息，再清模板；公开写作需要作者文风时，再补对象感、现场、机制和责任。

“像人”不等于故意粗糙。不要靠错别字、网感词、口头禅、夸张吐槽伪装真人，也不要把专业内容改浅。

## 资源导航

- 诊断或改写时，读取 `references/ai-bingzao.md`。
- 用户明确要求清理空话套话、文本模板密度很高，或要迭代病灶库时，再读取 `references/ai-cliche-library.md`。该文件只用于识别，禁止从中挑句式生成正文。
- 处理广播站、口播、公众号或“按我的文风”任务时，再读取 `references/broadcast-style.md`。
- 用户明确要求“快刀青衣文风”“像我早期文章”“广播站个人文风”时，再读取 `references/kuaidao-voice.md`。
- 短聊天、状态同步、技术文档通常只需要主流程和 `ai-bingzao.md`。

## 核心原则

- 保住事实、数字、日期、术语、引用、专名、限定条件和责任主体。
- 保住证据关系和推断强度：已确认事实、官方说法、作者判断和推测不能互相升级。
- 保住作者已有的第一人称经历、研究过程、犹豫和判断来源。
- 先改结构和句式，再改词汇；避免机械同义词替换。
- 只从原文和用户提供的材料中长出判断，禁止补虚构来源、经历、情绪和案例。
- 专业文本可以保持专业。清理目标是模板化、表演化、无源权威、信息复读和假深刻。

## 新增信息红线

改稿只能重排、压缩、澄清和重述已有信息。原文材料空，改后版本可以很短，也可以明确说明“原文缺少可核查细节”。禁止为了自然、具体和完整而用常识补内容。

重点防两类偷渡：

- 原文只有“提升效率、改善体验、促进公平”，改稿不得自行写成备课更快、反馈更及时、资源覆盖更多人。
- 原文只有“评测和数据岗位增加”，改稿不得自行写成模型瓶颈、岗位职责、公司战略或下一阶段目标。

改后稿新增的具体人物、动作、对象、结果、机制和因果，都必须在原文或用户提供的材料里找到依据。

## 场景、力度和范围

| 场景 | 默认力度 | 默认范围 | 注意 |
| --- | --- | --- | --- |
| 聊天回复 | minimal | structural | 只砍客套、谄媚和重复，不输出长诊断。 |
| 状态/汇报 | minimal | in-place | 允许系统主语，保留交付事实。 |
| 技术/产品文档 | minimal | in-place | 术语、字段、接口、报错优先保真。 |
| 公开写作 | standard | bounded | 清模板、查信息增量，再判断是否补对象感。 |
| 广播站/口播/公众号 | standard | bounded | 去味后读取广播站文风规则。 |
| 学术/正式公文 | minimal | in-place | 保留正式语体和常规被动，只清明显套话。 |

力度：

- `minimal`：只处理局部模板、重复、收尾腔和多余修辞。
- `standard`：统一语域，重排局部结构，压缩无信息段落。
- `aggressive`：模板密集或用户明确要求重写时使用。技术、学术和正式公文默认不启用。

范围：

- `structural`：允许删句、并句和重排，适合短文本。
- `bounded`：允许直接删除纯空话、同义复读和不承载事实的总结句。删除独有事实、完整案例、重要支线或改变论证结构时，放入“建议删除（待确认）”。
- `in-place`：只做句内降调和清晰化，整句删除只标建议。

保护区包括：数字、日期、术语、引用、命令、接口名、参数名、字段名、日志、报错、组织名、人名、书名、论文名、正式口径、系统行为主语。

## 固定流程

### 1. 先做内部锚点清单

改写前在内部列四项，不向用户展示：

1. 事实锚点：数字、时间、专名、引用、来源、限定条件和证据等级。
2. 作者锚点：原文已有的经历、研究动作、第一反应和判断。
3. 主线句：这篇文字最终要说明的一件事。
4. 段落功能：每段新增了什么。

找不到作者材料时，不要生成第一人称经历。可以在诊断区提示一个适合补真实经历的位置。

### 2. 按病灶表诊断

读取 `references/ai-bingzao.md`，只抓影响最大的 1 至 5 个问题。优先级依次为：

1. 事实和责任主体。
2. 对象与主线。
3. 信息增量。
4. 作者材料来源。
5. 套话和句式密度。
6. 节奏与排版。

不要见词就杀。词只是线索，真正的问题是它有没有遮住事实、拖慢推进或制造假深刻。

### 3. 检查信息增量

每段至少完成一项新任务：

- 增加一个事实。
- 展开一个机制。
- 补充一段真实经历或研究动作。
- 推进一个判断。
- 给出一个具体动作。
- 完成必要转场。

相邻两段只在重复同一判断时，合并或删除后一段。后半部分连续感慨、重复意义和换说法总结，压到一次即可。

### 4. 做减法改写

按顺序处理：

1. 删除空开场、聊天残留、无源权威和万能结尾。
2. 删除不承载事实的重复总结、感慨膨胀和名单式堆叠。
3. 把虚假主语换成真人、团队、机构、系统或流程。
4. 打散排比、对称填充、破折号穷举和机械递进。
5. 原文有支撑时，把抽象判断落到动作、结果、场景或责任关系。原文没有细节时，保留克制判断或删除空壳，禁止用常识补案例。
6. 统一语域。

`bounded` 模式下，纯空话和同义复读可以直接删除。涉及独有事实、完整案例、重要支线或立场变化时，正文先保留，并在改稿后列出“建议删除（待确认）”。

### 5. 补回作者文风

广播站、口播、公众号和个人文风任务，从材料已有内容中选择四类元素。它们是选项，不是必须凑齐的配额：

1. 对象感：听众是谁，他为什么需要听。
2. 作者来源：只保留或重述原文已有的读、试、问、查、对比和亲历。
3. 故事到机制：材料已经给出机制时，先让人进入事情，再讲目标、激励、限制、代价和责任；材料没有机制时跳过。
4. 合适的收束：从判断、动作、场景、悬念中选一种。

作者在场分三级：

- 原文有明确经历或研究动作：重点保护，可以调整说法。
- 原文只有明确判断：保留判断，不补经历。
- 原文没有第一人称材料：正文保持原视角，必要时在诊断区提示作者补充。

硬规则：禁用“否定 A、再转向 B”的固定对照句。原文已有时，优先改成直接判断、两句因果或具体动作。引用、标题、法律和政策原文属于保护区。

### 6. 两遍回读

第一遍查保真：

- 事实锚点是否完整。
- 判断和动作能否追溯到原文。
- 相关关系有没有被升级成因果、战略、瓶颈或趋势。
- 抽象词有没有被擅自扩写成原文不存在的场景和事实。
- 改后稿出现的新人物、动作、对象、结果和因果能否逐项在原文定位。
- 责任主体是否准确。
- 作者经历有没有被删薄或凭空增加。

第二遍查残留：

- 每段是否有信息增量。
- 开头有没有空背景。
- 中段有没有套话、伪路标和同义复读。
- 后半段有没有感慨膨胀。
- 结尾是否自然停住。
- 句长和段长是否有呼吸，不做机械切段。

第二遍只做轻量修正，不重写全文。

## 套话候选持续收集

`references/ai-cliche-library.md` 保存已经确认的常见模板。新发现先进入本地候选日志，避免单篇误判直接污染规则。

仅在以下情况记录：

- 用户明确指出某句有 AI 味。
- 病灶库尚未覆盖，且这个骨架具有明显复用性。
- 同类骨架再次出现在独立文本中。

记录要求：

- 只留抽象骨架，姓名、机构、日期、数字和具体事实改成 `____`。
- 每个文本最多记录 2 条新候选。
- 不保存原文长句、私人信息、引用内容和受保护文本。
- 没有新模式时不写日志。

使用活动技能目录下的脚本：

```bash
node scripts/cliche-log.mjs add \
  --pattern "抽象后的套话骨架" \
  --family "病灶族" \
  --action "默认处理动作" \
  --source "文本类型"
```

广播站任务可把日志写入：

```bash
node scripts/cliche-log.mjs add ... \
  --file "/Users/luojilab/Personal/01-写作/00-广播站/.qu-ai-wei-baselines/ai-cliche-candidates.json"
```

脚本会按骨架去重并累计次数。候选达到 3 次后，在用户要求迭代技能时人工复核，再晋升到 `ai-cliche-library.md`。候选日志不会自动改技能，也不会自动推送 GitHub。

## 广播站基线

处理 `/Users/luojilab/Personal/01-写作/00-广播站` 下的 Markdown 并写回正文后，立刻保存 AI 改稿基线：

```bash
node .qu-ai-wei-baselines/baseline-tool.mjs save "源文件绝对路径" --skill qu-ai-wei
```

只在完成写回后保存；诊断、建议和聊天内改写不保存。不覆盖旧基线。最终只需说明“已保存 AI 改稿基线”。

## 输出合同

### 默认改稿模式

只给一个推荐版本。文本已经自然时，直接说明“不需要改”，最多给一两处可选微调。不要附完整审稿过程。

### 诊断模式

用户说“先标问题”“只审不改”“哪里像 AI”时，只输出 1 至 5 个问题。每条包含：问题族、触发点、建议动作、是否建议改写。

### 对照模式

用户说“删改对照”“删改痕迹”“看看改了什么”时：

- 短文本使用 `~~删除~~` 和 `**新增**` 标记。
- 长文按改动段落列“原文、改后、理由”，避免重复展示未改内容。
- 事实、立场和结构变化单独说明。

### 文件写回模式

写回文件时，正文只保留成稿，不插入诊断标题、删改符号或说明。聊天中简短汇报修改范围和验证结果。

## 快速检查

- 事实、数字、引用、术语和责任主体是否完整。
- 官方说法、作者判断和推测是否保持原有强度。
- 有没有否定转向式固定对照句。
- 每段是否增加了新信息。
- 后半段是否在重复感慨。
- 有没有虚构作者经历或第一反应。
- 有没有用常识补出原文没有的案例、机制和结果。
- 原文信息很少时，改后稿是否仍然保持克制和简短。
- 排比和破折号后是否堆抽象词。
- 结尾是否从判断、动作、场景、悬念中自然选了一种。
- 技术和正式材料有没有被改浅。
